<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to  "Book Review", "/", class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if !current_user %>
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>
        
        <% if current_user %>
          <% if current_user.image.attached %>
            <li class="nav-item">
              <%= link_to users_show_path, class: "nav-link" do %>
                <%= image_tag current_user.image, height: 27, width: 27 %>
              <% end%>
            </li>
          <% else %>
           <li class="nav-item">
            <%= link_to current_user.email, users_show_path, class: "nav-link" %>
           </li>
          <% end %>
          <li class="nav-item">
            <%= link_to "Sign out", destroy_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>